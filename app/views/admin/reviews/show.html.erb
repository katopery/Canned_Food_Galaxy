<div class="container">
  <div class="row offset-1 my-5">
    <h2 class="px-3 text-center bg-info">
      <%= @member.nickname %>さんのレビュー一覧
    </h2>
  </div>
  
  <div class="row my-4 text-center justify-content-center">
    <div class="col-6 d-flex align-items-center">
      <div class="col-4">
        <%= link_to admin_member_path(@member) do %>
          <%= image_tag @member.get_profile_image(100,100), class: "image" %><br>
          <%= @member.nickname %>  
        <% end %> 
      </div>
      <div class="col-4">
        レビュー数： <%= @member.reviews.count %>
      </div>
    </div>
  </div>
    
  <% @reviews.each do |review| %>
    <div class="row my-5 text-center">
      <div class="col-2">
        <%= link_to admin_canned_food_path(review.canned_food.id) do %>
          <div>
            <%= image_tag review.canned_food.get_image(200,100), class: "image" %>
          </div>
          <div>
            <%= review.canned_food.canned_name %>
          </div>
        <% end %>
      </div>
      <div class="col-2">
        賞味期限
        <%= render "public/reviews/rate_read_only.html.erb", review: review, type: 'expiry_date_rating' %>
      </div>
      <div class="col-2">
        おいしさ
        <%= render "public/reviews/rate_read_only.html.erb", review: review, type: 'taste_rating' %>
      </div>
      <div class="col-2">
        おつまみ
        <%= render "public/reviews/rate_read_only.html.erb", review: review, type: 'snack_rating' %>
      </div>
      <div class="col-2">
        アウトドア
        <%= render "public/reviews/rate_read_only.html.erb", review: review, type: 'outdoor_rating' %>
      </div>
      <div class="col-2">
        <%= link_to "レビュー詳細", admin_review_comments_path(review.id), class: "btn btn-info mb-2" %>
        <%= link_to "レビュー削除", admin_review_path(review.id), method: :delete, class: 'btn btn-danger' %>
      </div>
    </div>
  <% end %>
  
  <div class="row">
    <div class="mx-auto my-5">
      <%= paginate @reviews %>
    </div>
  </div>
</div>