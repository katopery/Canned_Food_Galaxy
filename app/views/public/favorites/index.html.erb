<div class="container">
  <div class="row my-5">
    <h2 class="px-3 text-center">お気に入り一覧(登録数: <%= @favorites.count %>)</h2>
  </div>
  
  <div class="row my-2">
    <% if @favorites.present? %>
      <% @canned_foods.each do |canned_food| %>
        <div class="col-lg-2 col-sm-3 col-6 text-center d-flex align-items-center justify-content-center">
          <%= link_to canned_food_path(canned_food) do %>
            <div>
              <%= image_tag canned_food.get_image(150,100), class: "image" %>
            </div>
            <div>
              <%= canned_food.canned_name %>
            </div>
          <% end %>
        </div>
        <div class="col-lg-2 col-sm-3 col-6 py-3">
          <%= render "/public/favorites/index", canned_food: canned_food %>
        </div>
      <% end %>
    <% else %>
      <div class="col my-5 text-center justify-content-center">
        <%= link_to members_my_page_path, class: 'nav-link' do %>
          <h3>お気に入り登録している缶詰が見つかりませんでした。</h3>
        <% end %>
      </div>
    <% end %>
  </div>
  
  <div class="row">
    <div class="mx-auto my-5">
      <%= paginate @favorites %>
    </div>
  </div>
</div>
