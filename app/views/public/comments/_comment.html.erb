<%= form_with model: comments, url: review_comments_path, local: true do |f| %>
  <div class="row mt-2 mb-5 justify-content-center">
    <div class="col-12 text-center">
      <%= f.text_area :content, autofocus: true, placeholder: "コメントをここに(100文字以内)", cols: "50", rows: "3" %>
      <%= f.submit "コメントを送信", class: "btn fixing-button" %>
    </div>
    <div class="col-12 mt-2 text-center">
      投稿画像は
      <%= label_tag(nil, class: "image-input-btn") do %>
        <%= f.file_field :image, autofocus: true, accept: "image/*", class: "image-form" %>
        画像アップロード
      <% end %>
      から登録してください。
    </div>
  </div>
<% end %>